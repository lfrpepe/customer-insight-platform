# Único driver PostgreSQL do projeto — usado tanto localmente (scripts de
# seed) quanto no Databricks (ver ADR-001 e ADR-005). Python puro, sem
# compilação/dependência de pg_config no sistema operacional.
pg8000==1.31.2

python-dotenv==1.0.1
Faker==26.0.0

# Backend — Fase 5 (ver ADR-006: FastAPI em vez de Flask)
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.2
jinja2==3.1.4
python-multipart==0.0.9   # necessário para o FastAPI ler dados de formulário HTML (Pinpad/Totem/Formulário Web)
